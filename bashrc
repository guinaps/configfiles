HISTFILESIZE=5000
HISTSIZE=5000
# prompt has an initial null title-escape-sequence so screen can show the name
# of current process (see screen's man page)
PS1='\[\ek\e\\\e[33m\][\!] \u@\h:\[\e[1m\]\W\[\e[0m\]\$ '

# setting vi bindings
set -o vi
